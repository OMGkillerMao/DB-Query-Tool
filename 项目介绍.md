# 数据库查询工具 (Database Query Tool)

## 项目简介

这是一个基于 Web 的数据库查询管理工具，支持 PostgreSQL 和 MySQL 数据库连接管理、元数据查看和 SQL 查询执行，并提供自然语言转 SQL 的智能功能。

### 核心功能

- 🗄️ **多数据库支持**：支持 PostgreSQL 和 MySQL 数据库连接
- 🔍 **元数据浏览**：查看数据库结构、表、字段和关系
- 💬 **自然语言查询**：使用中文或英文自然语言描述查询需求，自动生成 SQL
- 📊 **查询结果展示**：优雅的结果表格展示，支持排序和筛选
- 📤 **数据导出**：支持导出查询结果为 CSV 或 JSON 格式
- 🎨 **现代化界面**：基于 MotherDuck 设计系统的精美 UI

## 技术栈

### 后端
- **框架**：FastAPI (Python 3.12+)
- **数据库**：SQLite (应用数据)、PostgreSQL/MySQL (用户连接)
- **AI 集成**：OpenAI API (自然语言转 SQL)
- **工具库**：
  - SQLModel (ORM)
  - SQLGlot (SQL 解析)
  - Alembic (数据库迁移)
  - Pydantic (数据验证)

### 前端
- **框架**：React 18 + TypeScript
- **UI 库**：
  - Refine 5 (后台管理框架)
  - Ant Design 5 (组件库)
  - Tailwind CSS 4 (样式)
  - Monaco Editor (SQL 编辑器)
- **构建工具**：Vite
- **状态管理**：React Hooks

## 项目结构

```
w2/db_query/
├── backend/              # FastAPI 后端
│   ├── app/
│   │   ├── api/         # API 路由
│   │   ├── models/      # 数据模型
│   │   ├── services/    # 业务逻辑
│   │   ├── adapters/    # 数据库适配器
│   │   └── utils/       # 工具函数
│   ├── alembic/         # 数据库迁移
│   ├── tests/           # 后端测试
│   └── pyproject.toml   # Python 项目配置
│
├── frontend/            # React 前端
│   ├── src/
│   │   ├── components/  # React 组件
│   │   ├── pages/       # 页面组件
│   │   ├── services/    # API 服务
│   │   └── types/       # TypeScript 类型
│   └── package.json     # Node.js 依赖
│
├── fixtures/            # REST Client 测试文件
├── docs/               # 项目文档
├── Makefile           # 开发命令
└── README.md          # 项目说明
```

## 快速启动

### 环境要求

- **Python**：3.12 或更高版本
- **Node.js**：推荐使用最新 LTS 版本
- **uv**：Python 包管理器（`pip install uv`）
- **npm**：Node.js 包管理器

### 安装步骤

#### 1. 克隆项目（如果需要）
```bash
cd /path/to/project
```

#### 2. 安装依赖
```bash
# 安装所有依赖（后端 + 前端）
make install

# 或者分别安装
make install-backend  # 安装后端依赖
make install-frontend # 安装前端依赖
```

#### 3. 配置环境变量
```bash
# 复制环境变量示例文件
cp backend/.env.example backend/.env

# 编辑 backend/.env，添加你的 OpenAI API Key
# OPENAI_API_KEY=sk-your-api-key-here
```

#### 4. 初始化数据库
```bash
# 运行数据库迁移
make db-upgrade
```

### 启动开发服务器

#### 方式一：同时启动前后端（推荐）
```bash
make dev
```

这会同时启动：
- 后端服务器：http://localhost:8000
- 前端服务器：http://localhost:5173

#### 方式二：分别启动
```bash
# 终端 1：启动后端
make dev-backend

# 终端 2：启动前端
make dev-frontend
```

### 访问应用

打开浏览器访问：http://localhost:5173

### API 文档

后端启动后，可以访问：
- **Swagger UI**：http://localhost:8000/docs
- **ReDoc**：http://localhost:8000/redoc

## 主要功能使用指南

### 1. 添加数据库连接

1. 点击左侧边栏的 "添加数据库" 按钮
2. 填写数据库连接信息：
   - **名称**：连接的显示名称
   - **数据库类型**：PostgreSQL 或 MySQL
   - **主机**：数据库服务器地址
   - **端口**：默认 PostgreSQL 5432，MySQL 3306
   - **数据库名**：要连接的数据库名称
   - **用户名**：数据库用户名
   - **密码**：数据库密码

### 2. 浏览数据库元数据

- 在左侧边栏选择已连接的数据库
- 展开数据库树形结构，查看：
  - 表结构
  - 字段信息
  - 数据类型
  - 主键/外键关系

### 3. 执行 SQL 查询

#### 手动编写 SQL
1. 选择数据库后，在 "MANUAL SQL" 标签页中编写 SQL
2. 点击 "EXECUTE" 按钮执行查询
3. 查看结果表格

#### 自然语言查询
1. 切换到 "NATURAL LANGUAGE" 标签页
2. 用中文或英文描述查询需求，例如：
   - 中文："查询所有用户"
   - 英文："Show me all users"
3. 点击 "GENERATE SQL" 或按 `Ctrl/Cmd + Enter`
4. 系统自动生成 SQL 并切换到编辑器
5. 可以编辑生成的 SQL 后执行

### 4. 导出查询结果

执行查询后，可以导出结果：

- **CSV 导出**：点击 "EXPORT CSV" 按钮
- **JSON 导出**：点击 "EXPORT JSON" 按钮

对于大数据集（> 10,000 行），系统会显示警告提示。

## 开发命令

### 查看所有可用命令
```bash
make help
```

### 常用命令

```bash
# 开发相关
make dev              # 启动前后端开发服务器
make dev-backend      # 仅启动后端
make dev-frontend     # 仅启动前端

# 测试相关
make test             # 运行所有测试
make test-backend     # 运行后端测试
make test-frontend    # 运行前端测试

# 代码质量
make lint             # 运行代码检查
make format           # 格式化代码

# 数据库相关
make db-migrate MESSAGE="描述"  # 创建新迁移
make db-upgrade       # 应用数据库迁移
make db-downgrade REVISION=previous  # 回滚迁移
make db-history       # 查看迁移历史

# 清理相关
make clean            # 清理构建文件
make clean-backend    # 清理后端构建文件
make clean-frontend   # 清理前端构建文件
```

## API 测试

项目提供了 REST Client 测试文件，可以使用 VSCode 的 REST Client 扩展进行测试：

1. 安装 [REST Client 扩展](https://marketplace.visualstudio.com/items?itemName=humao.rest-client)
2. 打开 `fixtures/test.rest` 文件
3. 点击 HTTP 请求上方的 "Send Request"
4. 在 VSCode 面板中查看响应

## 项目特色

### 1. 智能自然语言转 SQL
- 支持中英文自然语言输入
- 基于 OpenAI GPT 模型
- 自动生成优化的 SQL 查询
- 支持查询解释

### 2. 现代化 UI 设计
- 采用 MotherDuck 设计系统
- 响应式布局
- 深色/浅色主题支持
- 流畅的交互体验

### 3. 高性能查询
- 连接池管理
- 查询结果缓存
- 异步查询执行
- 大数据集处理优化

### 4. 安全性
- SQL 注入防护
- 连接信息加密存储
- CORS 配置
- 输入验证

## 配置说明

### 后端配置 (backend/.env)

```bash
# OpenAI API 密钥（必需）
OPENAI_API_KEY=sk-your-api-key-here

# 数据目录（可选，默认 ~/.db_query）
DB_QUERY_DATA_DIR=~/.db_query

# 日志级别
LOG_LEVEL=INFO

# CORS 源
CORS_ORIGINS=*

# 查询配置
QUERY_DEFAULT_LIMIT=1000
QUERY_HISTORY_RETENTION=50

# 数据库连接池配置
DB_POOL_MIN_SIZE=1
DB_POOL_MAX_SIZE=5
DB_POOL_COMMAND_TIMEOUT=60
```

### 前端配置 (frontend/.env.local)

```bash
# API 基础 URL（默认 http://localhost:8000）
VITE_API_BASE_URL=http://localhost:8000
```

## 故障排除

### 常见问题

1. **后端启动失败**
   - 检查 Python 版本是否为 3.12+
   - 确认已安装 uv：`pip install uv`
   - 检查端口 8000 是否被占用

2. **前端启动失败**
   - 确认已安装 Node.js
   - 删除 node_modules 后重新安装：`make clean-frontend && make install-frontend`

3. **自然语言转 SQL 不工作**
   - 检查 OPENAI_API_KEY 是否正确配置
   - 确认 API Key 有足够的额度
   - 查看后端日志了解错误详情

4. **数据库连接失败**
   - 检查数据库服务器是否可访问
   - 确认用户名和密码正确
   - 检查网络连接和防火墙设置

## 项目状态

✅ **阶段 1 完成**：项目基础设施搭建
✅ **阶段 2 完成**：核心功能实现
✅ **阶段 3 完成**：自然语言和导出功能

## 技术文档

项目包含详细的技术文档：

- `docs/ARCHITECTURE_INDEX.md` - 架构索引
- `docs/ARCHITECTURE_REDESIGN.md` - 架构重新设计
- `docs/IMPLEMENTATION_GUIDE.md` - 实现指南
- `docs/CLASS_DIAGRAM.md` - 类图
- `docs/QUICK_REFERENCE.md` - 快速参考

## 许可证

本项目为 AI 训练营项目，仅供学习使用。

## 联系方式

如有问题或建议，欢迎在项目中提出 Issue。